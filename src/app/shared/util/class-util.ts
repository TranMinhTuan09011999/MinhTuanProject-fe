export class ClassUtil {
  /**
   * Create multi class Name into a element tag (HTML)
   * Ex: <table [ngClass]="[stripe?'m-table m-table--stripe':'m-table', !isColumnStyle?'display-flex':'']"></table>
   */
}
